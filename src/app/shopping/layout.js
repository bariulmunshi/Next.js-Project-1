export const metadata = {
    title: 'Shopping | Next Hero',
    description: 'Hero Next',
  }

const ShoppingLayout = ({ children }) => {
  return (
    <div>
      <h1>Shopping Layout-sidebar</h1>
      {children}
    </div>
  );
};

export default ShoppingLayout;
